{
  "name": "criteria-adapter-greeter-example",
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "build": "bun build --compile index.ts --outfile out/adapter && rm -rf proto && cp -R ../criteria-typescript-adapter-sdk/proto .",
    "build:linux-x64": "bun build --compile --target=bun-linux-x64 index.ts --outfile out/adapter-linux-amd64 && rm -rf proto && cp -R ../criteria-typescript-adapter-sdk/proto .",
    "build:linux-arm64": "bun build --compile --target=bun-linux-arm64 index.ts --outfile out/adapter-linux-arm64 && rm -rf proto && cp -R ../criteria-typescript-adapter-sdk/proto .",
    "build:darwin-arm64": "bun build --compile --target=bun-darwin-arm64 index.ts --outfile out/adapter-darwin-arm64 && rm -rf proto && cp -R ../criteria-typescript-adapter-sdk/proto .",
    "pretest": "mkdir -p out && bun run build",
    "test": "bun test",
    "clean": "rm -rf out proto",
    "vuln-scan": "osv-scanner scan source --config=osv-scanner.toml -r .",
    "deps:outdated": "bun outdated"
  },
  "dependencies": {
    "@criteria/adapter-sdk": "file:../criteria-typescript-adapter-sdk"
  }
}
